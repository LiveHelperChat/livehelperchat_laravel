<h4>This url is visible only if operator is logged. Requires `lhfront`,`default` permission.</h4>

<pre>{{print_r($user->getUserData(), true)}}</pre>

<h4>Very first chat should appear here</h4>

<pre>{{print_r($items, true)}} </pre>